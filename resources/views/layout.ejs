<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Realtime Waffle app</title>
    <link href="https://fonts.googleapis.com/css2?family=Lato:wght@300;400;700&display=swap" rel="stylesheet">
    <link href="https://unpkg.com/tailwindcss@^1.0/dist/tailwind.min.css" rel="stylesheet">
    <link rel="stylesheet"
        href="https://maxst.icons8.com/vue-static/landings/line-awesome/line-awesome/1.3.0/css/line-awesome.min.css">
    <link rel="stylesheet" href="/css/app.css">
</head>

<body>
    <nav class="container mx-auto flex items-center justify-between py-2 px-6">
        <div class="flex flex-row" style="width:7%;">
            <a href="/"><img src="/img/waffle-home-logo.jpg" alt="logo" style="border-radius: 4rem; "></a>
        </div>
        <div class="flex flex-row w-1/2">
            <a href="/" alt="logo" class=" font-serif text-3xl font-bold " style="margin-left: 2rem;">Waffle Daddy</p>
        </div>
        
        <div class="flex">
            <ul class="flex items-center">
                <li class="ml-6 font-bold text-xl pb-2"><a href="/">Menu</a></li>
                <% if(user) { %>
                <li class="ml-6">
                    <form id="logout" action="/logout" method="POST">
                        <a class="text-xl font-bold" onclick="document.getElementById('logout').submit()" href="#">Logout</a>
                    </form>
                </li>
                <% } else { %>
                <li class="ml-6 font-bold text-xl"><a href="/register">Register</a></li>
                <li class="ml-6 font-bold text-xl"><a href="/login">Login</a></li>
                <% } %>
                <li class="ml-6 justify-center"><a href="/cart" class="inline-block px-4 py-2 rounded-full flex items-center">
                        <span id="cartCounter"
                            class="text-white font-bold pr-2"><%= session.cart ? session.cart.totalQty : '' %></span>
                        <img class="justify-self-center" src="/img/cart.png" alt="">
                    </a>
                </li>
            </ul>
        </div>
    </nav>
    <%- body %>
    <footer></footer>
    <!-- sharing the library to home page  -->
    <script src="/socket.io/socket.io.js"></script>  
    <script src="/js/app.js"></script>
</body>

</html>